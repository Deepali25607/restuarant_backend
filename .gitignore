# dependencies
node_modules

# build / cache
dist
.cache

# env secrets — never commit
.env
.env.local
.env.*.local

# local sqlite dev artifacts (legacy — we now use Postgres in production)
prisma/dev.db
prisma/dev.db-journal
*.sqlite
*.sqlite-journal

# uploads (user-generated, kept out of git)
uploads/*
!uploads/.gitkeep

# logs
*.log
npm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
